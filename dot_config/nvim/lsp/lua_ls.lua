return {
    settings = {
        Lua = {
            workspace = {
                -- raise the "Too large file" / preload caps (bytes)
                preloadFileSize = 5000,
                maxPreload = 5000,
            },
        },
    },
}
